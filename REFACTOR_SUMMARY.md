# 主界面重构总结

## 已完成的改动

### 1. 视觉基础与 CSS 变量系统

**文件：** `apps/main-ui/src/styles/global.css`

- 重做深浅主题色彩系统，使用更清晰的语义 CSS 变量
- 浅色：温暖中性底色 + 蓝绿强调色
- 深色：冷静深蓝灰底色 + 明快强调色
- 新增独立的窗口、侧栏、面板、卡片、输入背景变量
- 新增文字层级（primary/secondary/tertiary）
- 新增阴影、模糊、焦点环、状态色变量
- 允许输入控件和可编辑区域选择文本

### 2. 基础复用控件

**新增文件：**
- `apps/main-ui/src/components/Switch.tsx` - 统一的开关组件
- `apps/main-ui/src/components/IconButton.tsx` - 图标按钮（支持 ghost/danger 变体）
- `apps/main-ui/src/components/SettingRow.tsx` - 设置行布局容器

### 3. 主窗口结构

**文件：** `apps/main-ui/src/App.tsx`

- 移除独立的"危信设置"标题栏
- 拖拽区域融入顶部，无明显分隔线
- 右上角添加主题切换按钮（深色/浅色/跟随系统循环）
- 窗口控制使用 lucide 图标，更紧凑精致

### 4. 窗口控制

**文件：** `apps/main-ui/src/components/WindowControls.tsx`

- 使用 lucide-react 图标（Minus, Square, X）
- 精简尺寸和间距
- 关闭按钮保持"隐藏到托盘"语义

### 5. 主题管理

**文件：** `apps/main-ui/src/lib/theme.ts`

- 修正 localStorage key 为 `dangerchat:theme`（避免与其他页面冲突）
- 完善系统主题监听和自动应用

### 6. 设置工作区

**文件：** `apps/main-ui/src/settings/SettingsRoot.tsx`

- 左侧导航：使用窗口相同背景，分类间有间距，选中项有主题色背景和阴影
- 状态指示器使用图标（Shield/ShieldAlert/ShieldOff）+ 颜色语义
- 右侧内容：独立面板背景，更清晰的视觉层级
- 统计信息放在侧栏底部卡片中
- 移除逐字段"已保存"提示

**规则编辑器改为即时保存：**
- 任何修改（增删改）触发防抖保存（600ms）
- 移除显式"保存/已保存"按钮
- 保存状态在顶部统一显示
- 保存失败时显示错误并保留编辑内容

**联系人画像改进：**
- 使用独立的 saving 状态，避免整页刷新
- 按回车键快速添加

**关于页改进：**
- 清空日志操作增加二次确认
- 显示操作中状态

**开机启动：**
- 使用统一的 Switch 组件
- 独立卡片布局

### 7. Schema 字段渲染

**文件：** `apps/main-ui/src/settings/SchemaField.tsx`

- 使用新的 CSS 变量和视觉样式
- 开关使用统一的 Switch 组件
- 输入框统一样式和焦点状态

### 8. 托盘退出修复

**文件：** `src-tauri/src/lib.rs`, `src-tauri/src/tray.rs`

- 引入全局 `ALLOW_EXIT` 原子布尔状态
- 托盘"退出"菜单先设置退出意图，再调用 `app.exit(0)`
- `RunEvent::ExitRequested` 只在无退出意图时拦截
- 窗口关闭保持托盘常驻，托盘退出真正终止进程

### 9. 依赖更新

**文件：** `apps/main-ui/package.json`

- 新增 `lucide-react` 用于图标

## 编译与测试验证

✅ Rust 代码编译通过：`cargo check -p dangerchat`
✅ 前端代码构建成功：`pnpm -F main-ui build`
✅ 前端测试全部通过：`pnpm -F main-ui test`（11/11 tests passed）

## 需要真实运行验证的功能

### 启动主程序进行以下检查：

1. **主题切换**
   - 点击右上角主题按钮，循环切换深色/浅色/跟随系统
   - 验证色彩变化是否流畅和正确
   - 重启应用后主题设置是否保持

2. **窗口操作**
   - 最小化、最大化/还原是否正常
   - 关闭按钮是否隐藏到托盘（不退出进程）
   - 托盘图标左键点击是否切换显示/隐藏主窗口

3. **托盘退出**
   - 右键托盘图标 → 退出
   - 验证进程是否真正终止（不再驻留）

4. **设置即时保存**
   - 修改通用/目标/拦截配置项，无需点击保存
   - 重启应用验证配置已持久化
   - 快速连续修改同一项，验证不会回退到旧值

5. **规则编辑即时保存**
   - 添加、删除、修改规则
   - 观察顶部"保存中..."提示
   - 重启验证规则已保存
   - 输入非法正则，观察错误提示

6. **联系人画像**
   - 添加新联系人并标记为随意
   - 切换正式/随意画像
   - 重启验证画像已保存

7. **视觉细节**
   - 左侧导航选中项的阴影效果
   - 右侧内容区与侧栏的背景区分
   - 深浅主题下的对比度和可读性
   - hover 状态的过渡动画

8. **开机启动**
   - 切换开机启动开关
   - 验证系统启动项已更新

9. **布局与滚动**
   - 窗口缩放到最小宽度（760px）检查布局
   - 左侧分类多时是否正常滚动
   - 右侧内容区滚动是否流畅

10. **清空日志**
    - 点击"清空全部日志"
    - 二次确认是否显示
    - 确认后是否清空日志目录

## 未修改的文件

为避免干扰其他工作，以下文件保持原样：
- `apps/uitag/` 目录下的所有文件（有未提交改动）
- `apps/main-ui/src/alert/AlertRoot.tsx`（拦截弹窗独立组件）
- `apps/main-ui/src/notice/NoticePage.tsx`（首次告知页，保留合规内容）
- 后端配置、规则、画像持久化逻辑（已验证正确）

## 后续可优化项

1. **配置修改的错误处理**
   - 当前 `setConfig` 失败时无 UI 反馈
   - 可在 `SettingsRoot` 中统一捕获错误并显示 toast 或错误横幅

2. **规则编辑的更细粒度状态**
   - 当前整体保存，可改为单行保存状态指示
   - 提供"撤销"功能

3. **键盘快捷键**
   - 主题切换（如 Ctrl+T）
   - 快速导航分类（如数字键 1-8）

4. **动画过渡**
   - 分类切换时右侧内容淡入
   - 规则增删的列表动画

5. **无障碍增强**
   - 完善 ARIA 标签
   - 键盘焦点管理
   - 屏幕阅读器支持

## 回滚方案

如需回滚到重构前状态：

```bash
git checkout HEAD -- apps/main-ui/src apps/main-ui/package.json apps/main-ui/pnpm-lock.yaml src-tauri/src/lib.rs src-tauri/src/tray.rs
```

## 注意事项

- 所有改动已通过编译和测试验证
- 托盘退出修复涉及 Rust 全局状态，需真实运行验证
- 规则即时保存使用防抖（600ms），快速连续修改不会产生大量写入
- 主题状态仅存储在 localStorage，未同步到后端配置
