//! 危信 M0 技术尖峰。
//!
//! 目的是在投入生产工程之前，量化验证四件事：
//!
//! - M0-A 键盘守卫：精确快捷键、按键配对抑制、一次性许可与物理二次按键。
//! - M0-B 输入法：组合/候选状态下必须透传。
//! - M0-C 显示器捕获：DXGI → WGC 显示器级 → GDI 降级链与 DWM 裁剪。
//! - M0-D 端到端延迟：物理按键到提示可见的暖态耗时。
//!
//! 红线（`docs/合规与风险说明.md`）：不接触目标进程/文件/数据，
//! 不调用任何输入注入 API，不使用窗口定向捕获，默认零网络。
//! 本 crate 是可丢弃原型，不进入发布分支。

pub mod domain;

#[cfg(windows)]
pub mod harness;

#[cfg(windows)]
pub mod platform;
