# M0 技术尖峰状态

> 更新时间：2026-09-13｜状态：进行中，Gate 0 **未通过**
>
> 本文件只记录已实测的结论。未运行或未达标的项目一律标注为未验证，不得据此推进 M1/M2。
> **合成验证不等于门槛通过**：本轮所有捕获、定位、OCR 与延迟数据均来自自有合成测试窗口，
> 不代表真实微信界面，也不构成 `docs/01` §16 要求的独立数据集结论。

## 结论

Gate 0 尚未通过。守卫判定逻辑与合成验证设施已就绪并全部达标，
但四项硬门槛中仍有三项缺少真实环境证据：

| 门槛 | 逻辑/合成验证 | 真实环境证据 |
|---|---|---|
| M0-A 键盘守卫 | PASS（46 项自动化测试） | **缺失**：无物理按键回调时延样本 |
| M0-B 输入法 | 状态发布路径已接入并有单测 | **缺失**：未做多输入法人工矩阵 |
| M0-C 捕获/定位/OCR | PASS（合成集） | **缺失**：无真实界面与独立标注集 |
| M0-D 端到端延迟 | 合成分析链 PASS，裕度不足 | **缺失**：不含物理按键与 UI 渲染 |

在这些证据补齐之前不创建 M1/M2 工程骨架。

## 运行环境

| 项目 | 实测值 |
|---|---|
| 操作系统 | Windows 11 Pro 26220.9472（x64） |
| Rust | 1.98.1，`stable-x86_64-pc-windows-msvc` |
| Python / uv | 3.12.1 / 0.12.3 |
| 显示器 | 单屏 1920×1080，DPI 120（125%） |
| OCR | RapidOCR ONNXRuntime 1.4.4，CPU |

**重要限制**：当前只有单显示器、单一 DPI 档位（125%）。
100/150/200% 缩放、多显示器、负坐标、旋转与 HDR 均**未覆盖**。

## M0-A 键盘守卫与一次性许可

### 已通过：判定逻辑

`cargo test --workspace`，共 46 项，全部通过（42 单元 + 4 闸门回放）。

| 验证项 | 结果 |
|---|---|
| 目标外 100,000 次事件零误阻止 | PASS |
| 已 arm 但非配置发送键 100,000 次零误阻止 | PASS |
| 10,000 次事务：零重复发送、零孤立 keyup、零第二事务 | PASS |
| 失败路径（捕获失败/切换会话/离开前台/用户取消/服务异常）零放行 | PASS |
| 精确快捷键：Enter / Numpad Enter / Ctrl+Enter 互不混淆 | PASS |
| 额外修饰键（Shift/Alt/Win）存在时透传 | PASS |
| 物理键身份按 `vkCode + scanCode + extended` 配对 | PASS |
| 所有非空闲状态抑制新一轮发送键且不排队 | PASS |
| 前台变化后配对 keyup 仍被抑制（不产生孤立事件） | PASS |
| 许可最长 10 秒、消费要求快照新鲜度 ≤300 ms | PASS |
| 快照陈旧/许可过期时抑制按键并要求重新按键，不补发 | PASS |
| 覆盖批准与低风险许可分离，未批准不得签发 | PASS |
| 检测失败不得产生低风险许可 | PASS |
| 用户编辑使许可与分析结果失效 | PASS |
| 输入法组合态一律透传 | PASS |
| 外部注入事件不被抑制、不消费许可 | PASS |
| 服务不健康时停止保护而非假装受保护 | PASS |

### 未验证：真实钩子回调延迟

`m0 hook-latency` 已实现并能正常安装/卸载钩子，但需要真实物理按键才能采样。
**p99 ≤ 0.25 ms 与单次最大 ≤ 2 ms 两项门槛尚未测量。**

零样本时命令以非零状态退出，不会被脚本误判为通过。

复现命令（运行期间需正常打字）：

```
m0.exe hook-latency 30
```

## M0-B 输入法

**人工矩阵未执行。**

已实现并有单测覆盖的部分：

- IMM32 组合状态探测（`platform/ime.rs`）：组合串长度 >0 判为组合中；
  上下文缺失、查询出错或输入法已开启但无组合串时一律判为 `Unknown`。
- `Unknown` 与 `Composing` 都映射为"让给输入法"，这是安全默认值。
- 状态由协调线程周期采样后经命令队列发布给守卫，**钩子回调只读取已发布的布尔值**，
  回调内不调用 IMM32、不分配、不加锁。

仍需人工覆盖：微软拼音、五笔、英文键盘的候选框、组合文本、emoji 面板、
Enter 换行与快捷键反向配置。观察命令：

```
m0.exe ime-hook-probe 30
```

若长时间停在 `Unknown` 而无法进入 `Composing`，说明 IMM32 路径对该输入法
（可能使用 TSF）不可见，需改用 TSF 探测或把首期支持矩阵收窄为 Ctrl+Enter。

## M0-C 捕获、区域定位与 OCR

### 已通过：捕获与裁剪（合成）

`m0 capture-probe 40`，单屏 1920×1080、DPI 120：

| 指标 | 实测 | 门槛 | 结果 |
|---|---|---|---|
| 捕获裁剪 p50 | 20.5 ms | — | — |
| 捕获裁剪 p95 | 22.8 ms | ≤ 80 ms | PASS |

当前走的是降级链最后一级 GDI。DXGI Desktop Duplication 与 WGC 显示器捕获**尚未接入**。

### 已通过：区域定位（合成）

`m0 layout-eval`，自有合成测试窗口，8 个场景（4 种窗口尺寸 × 浅/深主题），
连续 4 次运行均为 8/8：

| 指标 | 实测 | 结果 |
|---|---|---|
| 关键 ROI 达标场景 | 8 / 8 | PASS |
| 最低 IoU（聊天标题） | 0.981 | PASS |
| 最低 IoU（消息上下文） | 0.996 | PASS |
| 最低 IoU（输入框） | 0.991 | PASS |
| 定位耗时平均 | 1.8 ms | — |

定位只使用灰度、边缘梯度与行列一致性，不依赖文字内容，也不使用目标检测模型。
结构签名由几何比例构成，不含任何文本。

**本轮修正的两个缺陷**：

1. 三个 Harness 此前用 `foreground_snapshot()` 取窗口，会在终端抢占焦点时
   捕获到错误窗口。改为按已知 HWND 读取快照并校验可见性。
2. 捕获前只用固定 `sleep` 等待绘制，导致 GDI 屏幕级捕获读到半绘制帧，
   定位随机失败（`NoSidebarBoundary`/`NoComposeSeparator` 在不同运行中命中不同场景）。
   改为等待 `WM_PAINT` 计数增长后再捕获；超时按失败处理，不重试掩盖。

此前记录的"DWM 扩展边界导致标题栏被误判"已修正为按客户区
（`GetClientRect` + `ClientToScreen`）裁剪。

### 已通过：OCR（合成集，非独立数据集）

`m0 ocr-export` 导出 8 场景 × 2 区域 = 16 张 ROI，
再由 `python -m dc_worker ocr-eval` 评测：

| 指标 | 实测 | 门槛 | 结果 |
|---|---|---|---|
| 标题精确匹配率 | 1.000（8 样本） | ≥ 0.99 | PASS |
| 草稿语料 CER | 0.0000（8 样本） | ≤ 0.02 | PASS |

CER 口径为总编辑距离 / 总真值字符数（语料级），同时报告最差样本。
比较前做空白归一化，依据 `docs/02` §12.1。

**这不能替代验收**：8 个合成场景使用固定字体与固定文案，
`docs/01` §16 与 `docs/03` §9 要求的独立、按组切分的标注集尚不存在。

### 未验证：DPI 与多屏矩阵

本轮只覆盖单屏 125%。100/150/200% 缩放、多显示器、负坐标、旋转、HDR 均未覆盖。

## M0-D 端到端延迟

### 合成分析链达标，但**门槛未通过**

`m0 e2e-latency 30`（前 5 轮预热不计入），三次运行：

| 运行 | p50 | p95 | p99 |
|---|---|---|---|
| 1 | 692.8 ms | 799.1 ms | 829.4 ms |
| 2 | 633.5 ms | 673.5 ms | 678.4 ms |
| 3 | 634.2 ms | 718.2 ms | 758.3 ms |

链路覆盖：显示器捕获 → 结构定位 → 内存 PNG 编码 → 保活 OCR 子进程 → 固定规则决策。

**为什么不能判定 M0-D 通过**：

- 不含物理按键、钩子回调、协调线程排队；
- 不含风险提示 UI 的渲染与呈现；
- p95 最差一次为 799.1 ms，距 800 ms 门槛不足 1%，
  任何额外阶段都极可能使其超标。

`docs/01` §15 的"100 ms 内出现处理中反馈"同样**无任何数据**。

### OCR 检测分辨率标定

初次测量 p95 为 1656.7 ms（FAIL）。定位后确认瓶颈完全在 OCR：
捕获 + 定位约 20 ms，OCR 单次约 1.6 s（标题 693 ms + 草稿 919 ms）。

在合成集上比较文本检测长边上限：

| `det_limit_side_len` | 标题精确匹配 | 草稿语料 CER | 单次 p50 |
|---|---|---|---|
| 736（默认） | 1.000 | 0.0000 | 997 ms |
| 480（当前） | 1.000 | 0.0000 | 399 ms |
| 320 | 1.000 | 0.0000 | 167 ms |

三档精度相同。选择 480 而非最快的 320，是为对更小字号保留分辨率余量。
该参数只影响检测阶段分辨率，不改变识别模型；
**真实界面字号与之不同时必须重新标定**。

## 合规验证

| 项目 | 结果 |
|---|---|
| 红线静态扫描（`scripts/redline_scan.py`） | PASS，27 个源文件 |
| 源码中不存在 `SendInput`/`keybd_event`/`mouse_event` | 确认，且**无任何白名单例外** |
| 源码中不存在目标进程内存/模块访问 API | 确认 |
| 源码中不存在 `PrintWindow`/UIA/窗口定向捕获 | 确认 |
| 默认零网络 | 确认，尖峰不含任何网络代码 |
| OCR 子进程不接受调用方文件路径 | 确认，只传内存 PNG 帧 |
| 不落盘任何截图或文本 | 确认，E2E 链路 ROI 只在内存中存活 |

`artifacts/m0-ocr/` 下的 PNG 是 OCR 评测输入，全部为合成内容，且已列入 `.gitignore`。

尚未执行：Process Monitor / API Monitor 的运行时取证。

## 下一步

按依赖顺序：

1. 运行 `m0 hook-latency 30` 并配合真实打字，补齐 M0-A 的延迟门槛。
2. 运行 `m0 ime-hook-probe 30`，完成 M0-B 多输入法人工矩阵。
3. 在 M0 阶段补一个最小提示窗口，测量物理按键到提示可见的完整 M0-D 延迟，
   以及 100 ms 处理中反馈。**不得推迟到 M1**。
4. 用授权、独立标注的真实界面 ROI 复跑 OCR 评测，并重新标定检测分辨率。
5. 在 100/150/200% DPI 与多显示器环境复跑 M0-C 全矩阵。
6. 执行 Process Monitor / API Monitor 运行时取证。

任一项无法达标时暂停并报告，不以"后续优化"推进 M1。
